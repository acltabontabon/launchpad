Write 2-5 bullets explaining the build profiles for an AI coding agent.

Rules:
- Output ONLY bullets. Each bullet starts with "- " followed by plain prose.
- Mention only profile ids, flags, plugins, and goals visible in the evidence.
- Each bullet must reference a specific build arg, plugin goal, or activation flag from the raw XML.
- Do not wrap whole lines in backticks.
- Do not include another "- " inside the bullet body.
- Do not invent profile behavior.
- Max 110 words.

GOOD: "- `pgo-instrument` invokes `native-maven-plugin` with `--pgo-instrument` to build an instrumented binary that collects an iprof profile when exercised."
GOOD: "- `pgo-optimized` consumes `${project.basedir}/profiles/default.iprof` and builds a tuned binary with `-O3`."
BAD:  "- `pgo-instrument` builds an instrumented binary for profiling."  (no concrete flag, no plugin reference)

Profiles (one per line, "id (activation) flags..."):
{profiles}

Raw <profile> XML blocks:
{profileXml}
