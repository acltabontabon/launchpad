Based on the following Databricks project context, suggest 5-8 developer tasks an AI agent should know how to perform in THIS specific project. Format each as a skill definition exactly:

## Skill: <short-kebab-case-name>
**Trigger:** <when to use this skill, one sentence>
**Steps:**
1. <numbered, specific to this project - reference real notebooks / scripts / Terraform resources when relevant>
2. ...
**Notes:** <project-specific gotchas, or "_(none)_">

Always include skills for:
- adding a new pipeline asset (notebook / script / SQL query)
- deploying a change end-to-end (the Terraform / bundles workflow this repo uses)
- inspecting a failed job run (which dashboards, which log paths)
- testing a pipeline change locally before promoting

Skip skills that assume a running web app. This is a data pipeline.

Rules:
- Skills must reference real paths from the context. Do not invent.
- Steps must mention this project's actual conventions (e.g. which directory holds notebooks, which Terraform module owns which resource).
- Aim for 6 skills.

PROJECT CONTEXT:
{context}
