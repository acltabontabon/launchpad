Based on the project context below, write concise, actionable engineering rules for any AI coding agent (Claude Code, Cursor, Copilot, Aider, etc.) working in this specific Databricks pipeline project.

Output format - one bullet per rule:

- **<short rule title>**: <one-sentence actionable instruction>

Always include rules covering:
- where new pipeline assets (notebooks / scripts / SQL) belong (use the actual paths from the context)
- how this project deploys to Databricks (Terraform-driven or Asset Bundle-driven; reference the actual file paths)
- secret handling (Databricks secret scopes vs Terraform variables vs env vars - reference what the project actually does)
- table / schema naming conventions visible in the context
- testing approach used here (local PySpark? Databricks Connect? notebook re-run? expectations?)
- idempotency expectations for re-running jobs

Rules must be:
- Specific to THIS project. Reference real paths from the context.
- Actionable (no "consider").
- Tool-agnostic: describe what the pipeline requires, not which AI tool is reading the rule.
- Ordered by importance.
- 8-15 bullets total.

Do not invent paths, table names, or conventions.

PROJECT CONTEXT:
{context}
