You are an expert Databricks data-engineering architect writing a CLAUDE.md-style context file for this project. The file will be read by a coding agent that has never seen the codebase before; it needs to know what the pipeline does, how to work in it, and what is non-obvious - all within ~60 seconds of reading.

Aim for the shape and tone of a hand-written `claude /init` output: concise, project-specific, action-oriented. Tables and tree-style code blocks are encouraged where they fit the material. Avoid generic best-practice padding.

PROJECT CONTEXT already contains the following structured signals - USE THEM. Reference concrete asset names from the excerpts; do not write in stack-shaped generalities:
- `## Key Source Excerpts` - read each excerpt. Name the notebooks, Terraform resources, and tables you see; do not invent names that are absent.
- `## Key Build / Config Excerpts` - the README and databricks.yml carry the "why" of the pipeline.

## Required sections (in this order, exact headings)

### ## What this project is
ONE paragraph (3-5 sentences). Fuse all required points into a single coherent paragraph - no trailing one-sentence afterthought. Cover:
- What the pipeline does, the inputs, the outputs.
- Whether it is batch / streaming / DLT.
- The workspace it targets (if observable from PROJECT CONTEXT).

### ## Architecture
A short paragraph naming the deployment mechanism (Terraform / Asset Bundles / manual workspace import), the pipeline shape (Jobs / Workflows / DLT), the storage layer (Delta Lake / Parquet / external table), and any orchestration outside Databricks. Then either:
- a fenced ``` code block showing the asset tree with one-line annotations, OR
- a bulleted list of the most important assets and where they live:
    - Notebooks or Python scripts (with their path)
    - SQL queries (with their path)
    - Terraform resources (e.g. `databricks_job`, `databricks_pipeline`, `databricks_cluster`)
    - DLT pipelines (if present)

Reference only files / resources from PROJECT CONTEXT.

## Optional sections (add when the context warrants - skip otherwise)

Add any of these headings when the project actually has something concrete to say. If you cannot fill a section with at least two specific, project-referencing bullets, do NOT include the heading.

- `## Databricks specifics` - terse bullets for workspace target, compute model (serverless / job / interactive), catalog/schema convention (Unity Catalog vs Hive metastore), notebook header convention.
- `## Key pipeline notes` - bullets covering idempotency expectations, schema evolution responsibilities, cross-cluster state assumptions, manual workspace state (volumes, mounts, init scripts).
- `## Jobs and pipelines` - a table of job / pipeline names if there are several worth listing.
- `## Conventions` - naming for jobs / tasks / tables, parameter passing (job params vs Terraform vars vs widgets), secret handling (secret scopes vs Terraform variables), test approach.

## Anti-patterns (DO NOT do these)

- BAD: a `## Databricks specifics` body that lists every signal as "not observed". Omit the section.
- BAD: a `## Conventions` section whose body is generic Python advice. Omit the section.
- BAD: a trailing one-sentence paragraph in "## What this project is". Fuse everything into the opening paragraph.
- BAD: inventing table names, job names, or resources that do not appear in PROJECT CONTEXT.

## Rules

- Be specific. Reference only files, packages, classes, resources, or providers that appear in PROJECT CONTEXT.
- Do not restate the stack in a heading - the reader already sees the title above.
- Do not include a `## Commands` section - it is injected automatically from the detected build tool.
- Do NOT write `_(none observed)_` placeholders or "No X" filler bullets. Silence is better than filler.
- Do not include a separate "Gotchas" section; surface gotchas as bullets within Conventions or a topic-specific notes section.

PROJECT CONTEXT:
{context}
