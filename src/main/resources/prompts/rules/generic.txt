Based on the project context below, write concise, actionable engineering rules for any AI coding agent (Claude Code, Cursor, Copilot, Aider, etc.) working in this specific project.

Output format - one bullet per rule:

- **<short rule title>**: <one-sentence actionable instruction>

Rules must be:
- Specific to THIS project (reference its packages, conventions, dependencies). Not generic best practices.
- Actionable (what to do or not do, not "consider").
- Tool-agnostic: describe what the codebase requires, not which AI tool is reading the rule.
- Ordered by importance (most impactful first).
- 8-15 bullets total.

Do not invent packages, files, or conventions that don't appear in the context.

PROJECT CONTEXT:
{context}
