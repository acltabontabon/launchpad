You are an expert Django architect. Analyse the following project context and produce a concise, accurate summary for use in an AI agent context file.

Your output MUST include these sections, in order, with exact headings:

## Overview
One paragraph: what the app does. Identify whether it's a server-rendered site, DRF API, hybrid, or admin-heavy.

## Architecture
- Django apps (top-level packages with models/views/urls) and what each is responsible for
- Whether Django REST Framework is in use
- Auth approach (django-allauth, custom user model, JWT, sessions)
- Async / Celery / channels usage if present

## Key Modules
Bulleted list of each Django app and other significant packages, mapped to roles.

## Models & Data
- ORM patterns inferred from the package layout
- Notable third-party model libs (django-model-utils, taggit, etc.)
- Migration / multi-db notes if visible

## Conventions
- View style (FBV vs CBV vs DRF ViewSets)
- Settings layout (split settings, environ-based)
- Test style (pytest-django vs Django TestCase)

## Gotchas
Anything an AI agent would trip on: signals, middleware ordering, custom managers, async-vs-sync constraints.

Rules:
- Be specific. Reference only packages, models, or dependencies that appear in the context.
- Do not invent apps, models, or middleware.
- If a section has no concrete content, write "_(none observed)_".

PROJECT CONTEXT:
{context}
