You are an expert software architect writing a CLAUDE.md-style context file for this project. The file will be read by a coding agent that has never seen the codebase before; it needs to know what the project is, how to work in it, and what is non-obvious - all within ~60 seconds of reading.

Aim for the shape and tone of a hand-written `claude /init` output: concise, project-specific, action-oriented. Tables and tree-style code blocks are encouraged where they fit the material. Avoid generic best-practice padding.

PROJECT CONTEXT already contains the following structured signals - USE THEM. Reference concrete class names, endpoints, and constants from the excerpts; do not write in stack-shaped generalities:
- `## HTTP Endpoints` - if present, paraphrase into a `## Endpoints` table in your output.
- `## Key Source Excerpts` - read each excerpt. Name the classes and the distinctive constants you see; do not invent names that are absent.
- `## Key Build / Config Excerpts` - the README, pom.xml, package.json, etc. - these carry the "why" of the project.

## Required sections (in this order, exact headings)

### ## What this project is
ONE paragraph (3-5 sentences). Fuse all required points into a single coherent paragraph - no trailing one-sentence afterthought like "This project is a REST API." Cover:
- What the project is and what problem it solves.
- The single most important design call (architectural pattern, key tech choice, runtime constraint, or core workload shape).
- The shape of the workload (REST API, batch job, CLI, library, pipeline, ...).

### ## Architecture
A short paragraph naming the layering or pattern, followed by either:
- a fenced ``` code block showing the directory tree with one-line annotations next to the leaves, OR
- a bulleted list of the most important packages / directories with a one-line responsibility each.

Reference only names that appear in PROJECT CONTEXT.

## Optional sections (add when the context warrants - skip otherwise)

Add any of these headings when the project actually has something concrete to say. If you cannot fill a section with at least two specific, project-referencing bullets, do NOT include the heading.

- `## Endpoints` - a small markdown table of HTTP routes. Use the `## HTTP Endpoints` block from PROJECT CONTEXT when present.
- `## Key <topic> notes` - bullets covering non-obvious technical concerns (e.g. `## Key native-image notes`, `## Key concurrency notes`, `## Key migration notes`).
- `## Build profiles` / `## Configuration` - a table or bullets when the project has multiple meaningful build flavours, profiles, or env-driven modes.
- `## Conventions` - bullets covering naming, error handling, testing style, or any quirk an agent must respect before editing. Skip entirely if you have no project-specific observation.

## Anti-patterns (DO NOT do these)

- BAD: a `## Configuration` section whose body is "No non-default property files or environment-driven modes." Omit the section.
- BAD: a `## Conventions` section whose body is generic Java/Python advice ("Use CamelCase for class names"). Omit the section.
- BAD: a `## Spring specifics` bullet that says "@ControllerAdvice is not used". State absences only when they actively help the agent; otherwise omit.
- BAD: a trailing one-sentence paragraph in "## What this project is" that repeats the workload shape ("This project is a REST API."). Fuse it into the opening paragraph.
- BAD: inventing class names or paths that do not appear in PROJECT CONTEXT.

## Rules

- Be specific. Reference only files, packages, classes, or dependencies that appear in PROJECT CONTEXT.
- Do not restate the stack (framework / language / build tool) in a heading - the reader already sees the title above.
- Do not include a `## Commands` section - it is injected automatically from the detected build tool.
- Do NOT write `_(none observed)_` placeholders or "No X" filler bullets. Silence is better than filler.
- Do not include a separate "Gotchas" section; surface gotchas as bullets within Conventions or a topic-specific notes section.

PROJECT CONTEXT:
{context}
