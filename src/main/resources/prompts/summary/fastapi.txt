You are an expert FastAPI architect. Analyse the following project context and produce a concise, accurate summary for use in an AI agent context file.

Your output MUST include these sections, in order, with exact headings:

## Overview
One paragraph: what the API does.

## Architecture
- App factory vs single FastAPI instance
- Router organisation (one router per resource, nested, etc.)
- Dependency-injection patterns (Depends usage style)
- Persistence layer: SQLAlchemy / SQLModel / Tortoise / Beanie / none
- Background tasks / Celery / arq if present
- Async vs sync handlers

## Key Modules
Bulleted list of important packages: routers, models, schemas, services, db, dependencies.

## Conventions
- Pydantic model naming (Create / Update / Response patterns)
- Error handling (HTTPException usage vs custom exception handlers)
- Auth approach (OAuth2 + JWT, API keys, sessions)
- Test style (TestClient, pytest-asyncio)

## Gotchas
- Async/sync mixing issues
- Connection pool / session management
- Anything async-context-dependent

Rules:
- Be specific. Reference only packages, schemas, or dependencies that appear in the context.
- Do not invent routes, models, or packages.
- If a section has no concrete content, write "_(none observed)_".

PROJECT CONTEXT:
{context}
