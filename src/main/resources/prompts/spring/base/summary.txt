You are an expert Spring / Spring Boot architect writing a CLAUDE.md-style context file for this project. The file will be read by a coding agent that has never seen the codebase before; it needs to know what the project is, how to work in it, and what is non-obvious - all within ~60 seconds of reading.

Aim for the shape and tone of a hand-written `claude /init` output: concise, project-specific, action-oriented. Tables and tree-style code blocks are encouraged where they fit the material. Avoid generic best-practice padding.

PROJECT CONTEXT already contains the following structured signals - USE THEM. Reference concrete class names, endpoints, and constants from the excerpts; do not write in stack-shaped generalities:
- `## HTTP Endpoints` - if present, paraphrase into a `## Endpoints` table in your output.
- `## Key Source Excerpts` - read each excerpt. Name the classes and the distinctive constants you see; do not invent names that are absent.
- `## Key Build / Config Excerpts` - the README and pom.xml / build.gradle excerpts carry the "why" of the project and any non-default Maven / Gradle profiles.

## Required sections (in this order, exact headings)

### ## What this project is
ONE paragraph (3-5 sentences). Fuse all required points into a single coherent paragraph - no trailing one-sentence afterthought like "This project is a REST API." Cover:
- What the project is and what problem it solves.
- The single most important design call.
- Whether it is a REST API, batch job, CLI, message-driven service, library / starter, or other shape.

### ## Architecture
A short paragraph naming the layering (controller / service / repository, hexagonal, modular monolith, ...) and any standout choice (reactive stack, GraalVM native, heavy @ConfigurationProperties use, event-driven internals). Then either:
- a fenced ``` code block showing the package tree with one-line annotations next to the leaves, OR
- a bulleted list of the most important packages with a one-line responsibility each.

Reference only packages from PROJECT CONTEXT.

## Optional sections (add when the context warrants - skip otherwise)

Add any of these headings when the project actually has something concrete to say. If you cannot fill a section with at least two specific, project-referencing bullets, do NOT include the heading.

- `## Endpoints` - a markdown table of HTTP routes (path, method, brief purpose). Use the `## HTTP Endpoints` block from PROJECT CONTEXT.
- `## Key native-image notes` - bullets covering GraalVM-specific concerns. Use only when the project targets native image.
- `## Build profiles` - table or bullets when the project has multiple Maven / Gradle profiles or build flavours (read them from the pom.xml / build.gradle excerpt).
- `## Spring specifics` - terse bullets for entry point, bean / configuration style, persistence stack, web stack, notable starters. Include only the ones the context confirms.
- `## Configuration` - bullets when there are non-default property files or environment-driven modes worth flagging.
- `## Conventions` - bullets covering naming, exception handling, validation, testing style (slice tests vs `@SpringBootTest`), profile usage, async / scheduled patterns, custom bean wiring. Skip entirely if you have no project-specific observation.

## Anti-patterns (DO NOT do these)

- BAD: a `## Configuration` section whose body is "No non-default property files or environment-driven modes." Omit the section.
- BAD: a `## Conventions` section whose body is generic Java advice ("Use CamelCase for class names"). Omit the section.
- BAD: a `## Spring specifics` bullet that says "@ControllerAdvice is not used". State absences only when they actively help the agent; otherwise omit.
- BAD: a trailing one-sentence paragraph in "## What this project is" that repeats the workload shape ("This project is a REST API."). Fuse it into the opening paragraph.
- BAD: inventing class names or paths that do not appear in PROJECT CONTEXT.

## Rules

- Be specific. Reference only files, packages, classes, or starters that appear in PROJECT CONTEXT.
- Do not restate the stack in a heading - the reader already sees the title above.
- Do not include a `## Commands` section - it is injected automatically from the detected build tool.
- Do NOT write `_(none observed)_` placeholders or "No X" filler bullets. Silence is better than filler.
- Do not include a separate "Gotchas" section; surface gotchas as bullets within Conventions or a topic-specific notes section.

PROJECT CONTEXT:
{context}
