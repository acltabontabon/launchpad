=== SKILLS ===

Include a skill named "add-cloud-config-property" that names:
- The actual properties file used in this project
- How @RefreshScope or @ConfigurationProperties is wired here
- Whether the Config Server is local or remote in dev

=== RULES ===

Add rules covering:
- **Config Server is the source of truth**: do not bake environment-specific values into application.properties when Spring Cloud Config is active.
- **@RefreshScope sparingly**: only on beans that genuinely need runtime config refresh; everything else stays singleton.
- **Feign clients are adapters**: place @FeignClient interfaces under the infrastructure package, not in domain code.
