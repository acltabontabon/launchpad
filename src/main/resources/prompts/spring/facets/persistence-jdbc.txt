=== SKILLS ===

Include a skill named "add-jdbc-query" that:
- Uses JdbcClient (preferred) or JdbcTemplate
- Always uses named parameters - never string concatenation
- Returns a typed record via RowMapper or a constructor reference
- Names the actual persistence package from the context

=== RULES ===

Add rules covering:
- **Parameterized queries only**: never concatenate user input into SQL. Use named parameters via JdbcClient.
- **Migrations own the schema**: schema changes go through Flyway/Liquibase, never via runtime DDL.
- **Records for rows**: map ResultSet rows into records, not Map<String, Object>.
