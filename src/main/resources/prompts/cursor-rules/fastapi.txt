You are writing .cursorrules for a Cursor AI user working in this FastAPI project. Based on the project context below, write concise, actionable rules.

Output format - one bullet per rule:

- **<short rule title>**: <one-sentence actionable instruction>

Focus areas:
- router organisation (where to add a new router)
- Pydantic schema naming
- dependency-injection conventions (auth, db session)
- async vs sync handler rule
- ORM session management
- error response pattern
- test client / pytest-asyncio specifics

Rules must be:
- Specific to THIS project. Reference real routers / packages.
- Actionable.
- 8-15 bullets total.

PROJECT CONTEXT:
{context}
