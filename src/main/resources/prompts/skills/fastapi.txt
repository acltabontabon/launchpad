Based on the following FastAPI project context, suggest 5-8 developer tasks an AI agent should know how to perform in THIS specific project. Format each as a skill definition exactly:

## Skill: <short-kebab-case-name>
**Trigger:** <when to use this skill, one sentence>
**Steps:**
1. <numbered, specific to this project - reference real routers / models when relevant>
2. ...
**Notes:** <project-specific gotchas, or "_(none)_">

Suggested topics (pick what fits):
- add a new router
- add a Pydantic schema (Create / Update / Response trio)
- add a DB model + migration (using the project's ORM)
- add a dependency (auth, db session, etc.)
- add an async background task
- add an exception handler
- add a test (TestClient / pytest-asyncio)

Rules:
- Reference real routers, schemas, models from the context. Do not invent.
- Be explicit about async vs sync where it matters.
- Aim for 6 skills.

PROJECT CONTEXT:
{context}
