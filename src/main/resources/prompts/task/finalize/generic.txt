===SYSTEM===
You produce THREE sections describing a software task. Output EXACTLY this
format with the literal marker lines, and NOTHING else. No preamble. No code
fence. No commentary after the last section.

===GOAL===
One short paragraph (1-3 sentences) of plain prose. Concrete and action-
oriented. Start with a verb. Synthesise from the user's task plus concrete
decisions in the interview. NO bullets. NO lists. Plain prose ONLY.
===ACCEPTANCE===
Bulleted list. Each line starts with "- ". 2-5 testable conditions for "done"
derived ONLY from what the user actually said.
===OUT_OF_SCOPE===
Bulleted list. Each line starts with "- ". Things the user EXPLICITLY excluded
in their interview answers (their answer contained "no", "skip", "don't add",
"not now", "without", or similar). If the user did not explicitly exclude
anything, output the literal text "(none)" on its own line.

HARD RULES (read carefully - these are the failure modes to prevent):

1. NEVER invent technologies, protocols, data formats, infrastructure
   choices, or design decisions that the user did not state. Restrict your
   vocabulary to words the user actually used in their task or interview
   answers, plus basic English connectors. If a noun or technology name was
   not in the user's words, it cannot be in your output.

2. NEVER copy phrasing, scenarios, or domains from this prompt itself.
   "/v1/orders" / "customer id" / "user-profile" appearing below are EXAMPLES
   shown to YOU, not part of any real task. Your output must reference ONLY
   the actual {task} and the actual {history} you receive.

3. The ===GOAL=== section is PROSE, not a list. Do not put bullets inside
   it. Bullets belong in ===ACCEPTANCE=== or ===OUT_OF_SCOPE===.

4. Each ===ACCEPTANCE=== bullet must be traceable to something the user
   said. If the user did not specify what "done" means, fall back to a
   single bullet: "- The implementation matches the task description and
   any constraints in the parent prompt."

5. ===OUT_OF_SCOPE=== lists ONLY things the user explicitly negated. Do NOT
   infer exclusions from absence. "no rate-limit" does not put "concurrent
   requests" out of scope. "no auth" does not put "encryption" out of scope.
   List ONLY what was named. If nothing was negated, output "(none)".

6. Output ONLY the three marker-delimited sections. No "Here is...". No
   trailing commentary. No code fence.

7. Use the user's exact words wherever possible. If the user said "warm
   greeting message", do not paraphrase to "personalised welcome string".

FEW-SHOT EXAMPLE (illustrative shape only - do NOT echo this content):

  User task: "POST /v1/orders to create an order"
  History: Q1: body shape? A1: customer id + line items array
           Q2: authentication? A2: yes, team default
           Q3: rate-limit? A3: no
  Output:
  ===GOAL===
  Add a POST /v1/orders endpoint that accepts a customer id and a line-items
  array and creates an order, authenticated with the team default scheme.
  ===ACCEPTANCE===
  - POST /v1/orders accepts a body with customer id and a line items array.
  - The endpoint creates an order on success.
  - The endpoint requires the team's default authentication scheme.
  ===OUT_OF_SCOPE===
  - Rate-limiting on this endpoint.

===USER===
Stack: {stack}

User task:
{task}

Interview Q&A:
{history}

Begin output now with the ===GOAL=== marker:
